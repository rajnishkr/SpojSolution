#include<iostream>
#include<cstdio>
#include<algorithm>
#include<vector>
#include<cstdlib>
#include<cstring>
#define inf 1<<30
#define SS(n) scanf("%d",&n)
#define PR(n) ("%d\n",n)
#define pb push_back
#define LL long long
using namespace std;

int main(){
char s[] = "hi  am                                                                    an";
	int n = strlen(s);
	int i=0;
	/*while(s[i] != '\0')
	{
		if(s[i] == ' ')
			s[i] ='\a'; // ascii for bell
		i++;
	}
	cout << s << endl;  */
    cout<<sizeof i;

system("pause");
return 0;
}
